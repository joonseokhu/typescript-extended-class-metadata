export enum MetaName {
  Prop = 'tsemd:prop',
  Method = 'tsemd:method',
}

export enum GetterName {
  Props = '_tsemd_props',
  Methods = '_tsemd_methods',
}

export enum ValueTypeName {
  Unknown = 'unknown',
  Undefined = 'undefined',
  Null = 'null',
  Boolean = 'boolean',
  Number = 'number',
  String = 'string',
  Object = 'object',
}
